<nav>
    <ul>
          {% if app.user %}
              {# lien page d'acceuil #}
              <li><a href="{{ path('main_home') }}" title="Home">Home</a></li>
              {# lien page profil (afficher si utilisateur enregistrer et connecter #}
              <li><a href="#" title="profil">profil</a></li>
              {# lien page de creation d'une publication #}
              <li><a href="{{ path('publication_create') }}" title="Créé une publication"> <i class="fas fa-plus-square"></i> </a></li>
              <li><a href="{{ path('app_logout') }}" title="logout">Se deconnecter ({{ app.user.nickname }}) </a></li>
          {% else  %}
              {# lien page de connection #}
              <li><a href="{{ path('app_login') }}" title="login">Se connecter</a></li>
              {# lien page d'inscription' #}
              <li><a href="{{ path('app_register') }}" title="register">S'inscrire</a></li>
            {# si l'utilisateur a la role Admin #}
            {% if is_granted("ROLE_ADMIN") %}
               <li><a href="{{ path('admin_dashbord') }}" title="admin dashbord"> Tableau de bord</a></li>

               {% endif %}




        {% endif %}
    </ul>
</nav>